<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vincent.pmsdemo.mapper.ZtProjectstoryMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.vincent.pmsdemo.entity.ZtProjectstory">
                    <id column="project" property="project" />
                    <result column="product" property="product" />
                    <result column="story" property="story" />
                    <result column="version" property="version" />
                    <result column="order" property="order" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            project, product, story, version, order
        </sql>

</mapper>
